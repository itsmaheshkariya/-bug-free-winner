name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Script
        run: | 
          npm i
          docker build -t app .
          docker login -u itsmaheshkariya -p a05605ce-8f5c-4ead-96b6-4067a8b7468d
          docker tag app test
          docker push itsmaheshkariya/test
          
